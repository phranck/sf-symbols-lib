`up` Move selection up in the grid
`down` Move selection down in the grid
`left` Move selection left
`right` Move selection right
`enter` Mark focused symbol as **selected**
`esc` Close drawer / Close About Box / Cancel actions
`cmd+f` Focus on the search field so you can start writing right away.